# AUDIT — 2026-09-10 (Thursday · Garde deny)

## Last 24h (short)

- **garde PR #21** merged to `main` at `7eb68c6`: control plane — PREVIEW/UNVERIFIED isolated from RECEIPT/VERIFIED; Human Decision Lock; `string true is still PREVIEW`; night manifest: preview, real paths, Carl lock — not a receipt.
- **ancrage-protocol PR #8** (`b8ae041`): PQC rail opt-in only, no fake ML-KEM, v0 immutable.
- **acorn-juge PR #31** (`34ea5b9`): sweep loop — READY only after ping; chaos 500/503/429/timeout never READY.
- **famille**: ots anchors + official-seal Φ = CHANNEL NOT PRESENT (`ccd9f61`); draft PR #306 open (`cx: Φ n'est pas une couleur`) — no second famille PR this hour.

Week rhythm today: **Thu Garde deny**.

## Chosen fact (ONE)

**String true is still PREVIEW.** — from garde PR #21 (`6f48f8e` / merge `7eb68c6`).

Deny lock: Preview ≠ receipt. Garde does not promote a preview flag into a receipt. Host only https://acorn-royal-dune-blend.grok.me. Unforge does not sign. Classique by default. ε=0 is a lie. QUANTUM off camera.
